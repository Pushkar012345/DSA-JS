class Solution {
    isSorted(arr) {
        // code here
      
       //logic:- traverse if arr[i]<arr[i-1] false t
       for(let i=0;i<arr.length;i++)
       {
           if(arr[i+1]<arr[i])
           {
               return false;
           }
       }
       
       return true;
    }
}
